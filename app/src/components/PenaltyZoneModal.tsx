import { ShieldAlert } from 'lucide-react';
import { useGameStore } from '@/stores/gameStore';

export function PenaltyZoneModal() { const { penaltyZone, dismissPenaltyZone, navigateTo } = useGameStore(); if (!penaltyZone) return null; return <div className="fixed inset-0 z-[80] bg-black/80 flex items-center justify-center p-5"><div className="glass-card max-w-sm p-6 text-center border-[#EF4444]/40"><ShieldAlert size={38} className="mx-auto text-[#EF4444] mb-3"/><p className="system-text text-[#EF4444]">PENALTY ZONE</p><h2 className="text-lg font-bold mt-1">Incomplete quests detected</h2><p className="text-sm text-white/55 mt-3">Reset your momentum with a recovery quest. You can always edit non-required quests to keep your system sustainable.</p><button onClick={() => { dismissPenaltyZone(); navigateTo('quests'); }} className="mt-5 px-4 py-2 bg-[#CBD5E1]/15 text-[#CBD5E1] rounded-lg text-sm">View recovery plan</button></div></div>; }
